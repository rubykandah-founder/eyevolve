{
  "name": "eyevolve",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "test:compile": "tsc -p tsconfig.test.json",
    "test:unit": "npm run test:compile && node --test .test-build/tests/feature/*.js",
    "test:e2e": "npm run test:compile && node --test .test-build/tests/e2e/*.js",
    "test": "npm run test:compile && node --test .test-build/tests/feature/*.js .test-build/tests/e2e/*.js"
  },
  "dependencies": {
    "next": "latest",
    "openai": "latest",
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {
    "@types/node": "latest",
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "typescript": "latest"
  }
}
